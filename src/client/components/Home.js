import React from "react";

const Home = () => {
  return (
    <div>
      <span>Hi there!!!</span>
      <button onClick={() => console.log("Button pressed")}>Press ME</button>
    </div>
  );
};

export default Home;
